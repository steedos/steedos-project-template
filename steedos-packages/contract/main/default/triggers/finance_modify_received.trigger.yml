name: finance_modify_received
listenTo: finance
when:
  - afterInsert
  - afterUpdate
isEnabled: true
handler: |-
  const { id, doc, spaceId, userId } = ctx.params;

  if (doc.actual) {    
    ctx.getObject('finance').update(id, {
      received:true
    })
  }

locked: false
